# Общие зависимости (пайплайн + агент + проект «глаза»)
torch>=2.0
numpy>=1.24

# Пайплайн дообучения ruGPT-3 Small
transformers>=4.38
datasets>=2.16
tokenizers>=0.15
tqdm>=4.66
pyyaml>=6.0

# Проект «Глаза для UE5»
pillow>=10.0
cairosvg>=2.7
pytest>=7.0

# Опционально: для скачивания корпуса через Croissant (иначе хватит datasets):
# mlcroissant
