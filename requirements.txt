# Ставится на ноуте (WSL2 / Linux) и на раннере.
# CUDA-сборка torch — отдельной командой, см. docs/SELF_HOSTED_RUNNER.md
torch>=2.0
transformers>=4.38
datasets>=2.16
tokenizers>=0.15
numpy>=1.24
tqdm>=4.66
pyyaml>=6.0

# Опционально: для скачивания корпуса через Croissant (иначе хватит datasets):
# mlcroissant

